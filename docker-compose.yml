version: '2.4'
services:
  db:
    image: mysql
    container_name: mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - data_of_devdojo_tests:/var/lib/mysql

volumes:
  data_of_devdojo_tests: